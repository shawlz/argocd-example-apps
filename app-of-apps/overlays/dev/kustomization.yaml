apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- "../../base"

patches:
  - target:
      annotationSelector: avantgarde.ng/environment-dev != deploy
    patch: |
      $patch: delete
      kind: ApplicationSet
      metadata:
        name: DOES NOT MATTER
  - target:
      labelSelector: avantgarde.ng/clusters == all
    patch: |
      kind: ApplicationSet
      metadata:
        name: dev-guestbook
      spec:
        generators:
        - list:
            elements:
            - cluster: in-cluster
              environment: dev
              path: kustomize-guestbook
        template:
          spec:
            project: default